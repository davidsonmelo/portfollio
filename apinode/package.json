{
  "name": "apinode",
  "version": "1.0.0",
  "description": "api com json serve",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "json-server --watch db.json"
  },
  "author": "davidson",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
